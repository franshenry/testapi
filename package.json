{
	"name": "naiklevel",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"directories": {
		"test": "test"
	},
	"scripts": {
		"test-api": "node ./node_modules/mocha/bin/mocha ./test --recursive --reporter mochawesome --reporter-options reportDir=reports/mochawesome/ --timeout 180000",
		"reports": "open './reports/mochawesome/mochawesome.html'",
		"test": "echo \"Error: no test specified\" && exit 1",
		"test-junit": "mocha --timeout 5000 --reporter mocha-junit-reporter"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"chai": "^4.3.7",
		"dotenv": "^16.0.3",
		"mocha": "^10.2.0",
		"mocha-junit-reporter": "^2.2.0",
		"mochawesome": "^7.1.3",
		"supertest": "^6.3.3"
	},
	"dependencies": {
		"chai-json-schema": "^1.5.1"
	}
}
